# wrap(文本换行)

```css
.wrap-break {
  word-wrap: break-word;
  hyphens: auto;
}
.wrap-truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.wrap-nowrap {
  white-space: nowrap;
}
```
